// Post-transfer support link — hosted builds only. The standalone build swaps
// this module for support.inline.ts (see build/use-inline-variants.ts).
export function supportLink(): HTMLElement | null {
  return null;
}
